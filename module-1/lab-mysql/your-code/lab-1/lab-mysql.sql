CREATE DATABASE lab_mysql;
USE lab_mysql;

CREATE TABLE Cars (Cars_id INT, VIN INT, Manufacturer VARCHAR(45), Model VARCHAR(45), Year INT, Color VARCHAR(45));

CREATE TABLE Customers (Cars_id INT, VIN INT, Manufacturer VARCHAR(45), Model VARCHAR(45), Year INT, Color VARCHAR(45));
